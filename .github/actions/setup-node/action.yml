name: Setup Node.js
description: Setup Node.js with version from .nvmrc and npm cache

runs:
  using: composite
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version-file: '.nvmrc'
        cache: 'npm'
