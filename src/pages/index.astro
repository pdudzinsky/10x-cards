---
import Welcome from "../components/Welcome.astro";
import Layout from "../layouts/Layout.astro";

// Note: We don't redirect authenticated users server-side because
// Bearer tokens are in localStorage (client-side only).
// The Welcome component can handle client-side redirect if needed.
---

<Layout>
  <Welcome />
</Layout>
